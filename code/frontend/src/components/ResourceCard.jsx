// Reusable card for a single resource listing. Not implemented — Phase 3.
export default function ResourceCard({ resource }) {
  // TODO: type, description, price, distance, "Book Now" button
  return null;
}
